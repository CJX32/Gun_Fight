#include "menu.h"
#include "options.h"
#include "functions.h"
#include "animations.h"
#include "PictiureDispose.h"
#include <time.h>
#include <stdio.h>
#include <conio.h>
int main(void) {
	/*Settings set;
	set_init(&set);
	int choice;
	choice=menu();
	if (choice == 1)
	{

	}
	else if(choice==2)
	options(&set);
	else if (choice == 3)
	{

	}
	else
	closegraph();
	*/
	/*int ch=0;
	
	while (1) {
		if (_kbhit())
		ch = _getch();
		printf("%d\n", ch);
	}*/
	int a = 0, b = 0;
	walk(&a,&b);
}